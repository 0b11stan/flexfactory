<!DOCTYPE html>

<html lang="fr">

  <head>
    <meta charset="utf-8">
    <title>> Tristan Pinaudeau_</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A very simple website showing my background, my projects and more.">
    <meta name="author" content="Tristan Pinaudeau">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="css/fontawesome.css">
    <link rel="stylesheet" href="css/normalize.css">
    <link rel="stylesheet" href="css/main.css">

    <style>
      /* background styles */

      #background-panel {
        grid-area: hp;
        display: grid;
        grid-template: 1fr 1fr 1fr 1fr 1fr 1fr 1fr;
        grid-template-rows: 1fr 3fr;
        grid-template-areas:
          "ch1 ch2 ch3 ch4 ch5 ch6 ch7"
          "cht cht cht cht cht cht cht";
      }

      .background-2015.panel-menu { grid-area: ch1; }
      .background-2016.panel-menu { grid-area: ch2; }
      .background-2017.panel-menu { grid-area: ch3; }
      .background-2018.panel-menu { grid-area: ch4; }
      .background-2019.panel-menu { grid-area: ch5; }
      .background-2020.panel-menu { grid-area: ch6; }
      .background-2021.panel-menu { grid-area: ch7; }
      .background-tiles {
        grid-area: cht;
        text-align: left;
        z-index: 50;
        background: #333333;
      }

      {% for year in background %}
        #background-{{ year }} { z-index: 0; }
        #background-{{ year }}.panel-menu:hover { font-weight: bold; }
        #background-{{ year }}:hover { z-index: 100; }
        #background-{{ year }}:hover ~ #background-{{ year }} {
          font-weight: bold;
          z-index: 100;
        }
      {% endfor %}

      .background-tile { position: relative; margin: 0 2rem; }
      .background-tile.offset-1 { margin-top: 5rem; }
      .background-tile.offset-2 { margin-top: 10rem; }

      .background-content {
        position: absolute;
        left: 0;
        right: 0;
        display: inline-block;
        background: #E5E5E5;
        color: #333333;
        padding: 1rem;
        border-radius: 10px;
        border-style: solid;
        border-color: #E5E5E5;
        border-width: 2px;
        margin: 0;
      }

      .background-info {
        position: absolute;
        right: 0;
        display: inline-block;
        background: #404040;
        color: #E5E5E5;
        padding: 1rem;
        border-top-right-radius: 10px;
        border-bottom-right-radius: 10px;
        border-style: solid;
        border-color: #7F7F7F;
        border-width: 2px;
        margin: 0;
      }

      .degree-done { background: #E5E5E5; }
      .degree-not-done {
        background: #AFAFAF;
        color: #888888;
      }
    </style>

    {#
    {% if selectedyear %}
      <link rel="stylesheet" href="css/background.css"/>
    {% else %}
      <link rel="stylesheet" href="css/index.css"/>
    {% endif %}
    #}

    <link rel="icon" type="image/png" href="img/favicon.png">
  </head>
  <body>
    <div class="container">
      {% for link in links %}
        <a id="cell-{{ link }}"
           class="links {{ links[link].icon }}"
           href="{{ links[link].value }}"
           {{ "download" if links[link].download else "" }}></a>
      {% endfor %}

      <div id="cell-title">
        <h1 id="title">
          <a href="/index.html" class="unanchor">
            $ tristan pinaudeau<span id="title-cursor">_</span>
          </a>
        </h1>
        <p id="title-infos">{ city: bordeaux; age: 22; has_car: true }</p>
      </div>

      <div id="cell-subjects">
        {% for subject in subjects %}
          <span class="subject">{{ subject }}</span>
        {% endfor %}
      </div>

      <div id="background-panel" class="panel">
        {% for year in background %}
          <h2 class="panel-menu background-{{ year }}
                     {{ "boldify" if selectedyear == year else "" }}">
            <a href="#" class="unanchor">{{ year }}</a>
          </h2>
        {% endfor %}
        {% for year in background %}
          <div id="background-tiles">
            {% for element in background[year] %}
              <div class="background-tile offset-{{loop.index - 1}}">
                {% if element[0] == "degree" %}
                  <h5 class="background-content">{{element[1]}}</h5><!--
                  --><h5 class="background-info">Diplômes</h5>
                {% elif element[0] == "experience" %}
                  <h5 class="background-content">{{element[1]}}</h5><!--
                  --><h5 class="background-info">Experiences</h5>
                {% endif %}
              </div>
            {% endfor %}
          </div>
        {% endfor %}
      </div>

      <div id="content-panel" class="panel">
        <div id="content-menu" class="row">
          <h4 id="projects-menu" class="panel-menu disabled">Projets</h4>
          <h4 id="readings-menu" class="panel-menu">Lectures</h4>
          <h4 id="articles-menu" class="panel-menu disabled">Articles</h4>
        </div>
        <div id="content-body">
          <div hidden id="projects-body"></div>
          <div hidden id="readings-body"></div>
          <div hidden id="articles-body"></div>
        </div>
      </div>

    </div>
  </body>

</html>
